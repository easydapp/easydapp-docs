{
  "name": "easydapp-docs",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "next build",
    "export": "next build && next export",
    "clean": "rimraf .next .turbo",
    "dev": "next",
    "start": "next start",
    "deploy": "pnpm run build && wrangler pages deploy --project-name easydapp-docs ./out"
  },
  "dependencies": {
    "katex": "^0.16.19",
    "next": "^14.2.5",
    "nextra": "^3.3.1",
    "nextra-theme-docs": "^3.3.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "dependenciesMeta": {
    "nextra": {
      "injected": true
    },
    "nextra-theme-docs": {
      "injected": true
    }
  },
  "devDependencies": {
    "wrangler": "^3.100.0"
  }
}